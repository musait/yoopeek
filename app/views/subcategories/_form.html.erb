<%= form_with(model: under_category, local: true) do |form| %>
  <% if under_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(under_category.errors.count, "error") %> prohibited this under_category from being saved:</h2>

      <ul>
      <% under_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
